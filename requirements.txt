# Training + dashboard dependencies (CPU-only; no GPU needed).
# Install torch from the CPU wheel index to avoid the 2.5GB CUDA download:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cpu
#   uv pip install -r requirements.txt
# torch itself is intentionally not pinned here: any >=2.4 CPU build works
# (fedora ran 2.4.1, macOS ran 2.13).
numpy>=1.26
pygame==2.6.1
matplotlib==3.11.0
tensorboard==2.21.0
flask==3.1.3
flask-sock==0.7.0
simple-websocket==1.1.0
imageio==2.37.3
imageio-ffmpeg==0.6.0
